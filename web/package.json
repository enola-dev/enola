{
  "name": "enola.dev",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "bundle": "esbuild ./script.js --bundle --format=esm --outfile=public/bundle.js",
    "serve": "npx http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "The Enola <https://enola.dev> Authors",
  "license": "Apache-2.0",
  "description": "Semantic Web Linked Data UI",
  "dependencies": {
    "graphology": "^0.25.4",
    "graphology-gexf": "^0.13.2",
    "graphology-layout": "^0.6.1",
    "graphology-layout-forceatlas2": "^0.10.1",
    "sigma": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "http-server": "^14.1.1"
  }
}
